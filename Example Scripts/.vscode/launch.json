{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Compile and Run Fearn Program",
            "type": "f5anything",
            "request": "launch",
            "command": "cd ${relativeFileDirname}\\build; FearnRun ${fileBasenameNoExtension} ${input:args}",
            "preLaunchTask": "Build Fearn Program"
        }
    ],
    "inputs": [{
        "id": "args",
        "type": "promptString",
        "description": "Enter command-line arguments"
    }]
}