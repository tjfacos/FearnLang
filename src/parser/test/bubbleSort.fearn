fn main() => void
    let
        l : int[7] = { 7, 6, 11, 23, 4, 1, 9 }
   
    print bubble(l)


fn bubble(l : int[]) => int[]
    let
        length : int = len(l)
        i : int = 1
        swapped : bool = True
        temp : int


    while swapped
        swapped = False
        while i < length:
            if l[i - 1] > l[i]
                temp = l[i - 1]
                l[i - 1] = l[i]
                l[i] = temp
                swapped = True
            i += 1
   
    return l













/* Define Keywords */


// Iteration Keywords
WHILE   :   'while'             ;
BREAK   :   'break'             ;
CONTINUE:   'continue'          ;

// Type Keywords
STRING  :   'str'               ;
INT     :   'int'               ;
FLOAT   :   'float'             ;
BOOL    :   'bool'              ;
VOID    :   'void'              ;

// Selection Keywords
IF      :   'if'                ;
ELSE    :   'else'              ;

// Function Keywords
FN      :   'fn'                ;
RETURN  :   'return'            ;



/* Define Operators */

/*
// Comparison
LESS        :   '<'             ;
GREATER     :   '>'             ;
LOE         :   '<='            ;
GOE         :   '>='            ;
EQUAL       :   '=='            ;
NOT_EQUAL   :   '!='            ;

// Boolean
AND         :   '&&'            ;
OR          :   '||'            ;
NOT         :   '!'             ;

// Arithmetic
ADD             :   '+'         ;
SUB             :   '-'         ;
MULT            :   '*'         ;
DIV             :   '/'         ;
MOD             :   '%'         ;
ADD_ASSIGN      :   '+='        ;
SUB_ASSIGN      :   '-='        ;
MUL_ASSIGN      :   '*='        ;
DIV_ASSIGN      :   '/='        ;
MOD_ASSIGN      :   '%='        ;
EXP             :   '^'         ;

*/
