import io
import from "StalinSort.fearn"

struct test {
    let x : int;
}

fn main(args : str[]) => void
{
    let list : int[] = new int[args.length()];

    for (let i : int = 0; i < args.length(); i++)
    {
        list[i] = (int)args[i];
    }

    print((str)list.stalinSort());

    print((str)new test(5).x);

}