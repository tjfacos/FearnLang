import io
import from 'Person.fearn'

fn main(args : str[]) => void
{
    let user : $Person = new Person(args[0], (int)args[1]);

    print(user.toString());
    print(user.name + "'s name is " + (str)user.name.length() + " characters long.");
    
}