import io
import random

import from "path/to/fearn/program"

struct MyStruct {
    let property1 : int;
    let property2 : str[];
}

fn main(args : str[]) => void
{
    let instance : $MyStruct = new MyStruct(5, new str[] {"HELLO", "WORLD"});
    print((str)instance.property1);
    print(instance.property2[1]);
}